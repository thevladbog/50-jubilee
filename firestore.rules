rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /rsvps/{document=**} {
      // For this simple event app, allow all reads and writes
      allow read, write: if true;
    }
  }
}
